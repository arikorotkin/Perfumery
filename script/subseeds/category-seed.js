'use strict'

const db = require('../../server/db')

const {Category, Note} = require('../../server/db/models')

async function seedNoteAndCategory(noteObj, categoryName) {
    try {
        return 0
    } catch(err) {
        console.error(err)
    }
}